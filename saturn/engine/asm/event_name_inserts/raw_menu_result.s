; Preserve the exact final X returned by raw_menu_name_insert and resume the
; shared choice-menu loop after its retired fixed-three-glyph calculation.
    mov     r0, r9
    bra     CONTINUE
    add     #2, r10
